{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://viewer.local/schemas/mesh/focus_view.payload.json",
  "title": "focus_view payload",
  "description": "Payload for the viewer node `focus_view` surface. Brings an open view to the foreground.",
  "type": "object",
  "required": ["id"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Id of the view to focus (matches View.id)."
    }
  }
}
